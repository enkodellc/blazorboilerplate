@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{BlazorBoilerplate.Shared.Settings.LoginPath}/{System.Net.WebUtility.UrlEncode(Navigation.Uri)}", true);
    }
}

@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{BlazorBoilerplate.Shared.Settings.LoginPath}?returnurl={System.Net.WebUtility.UrlEncode(Navigation.Uri)}", true);
    }
}

@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{BlazorBoilerplate.Shared.Settings.LoginPath}?returnurl={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}

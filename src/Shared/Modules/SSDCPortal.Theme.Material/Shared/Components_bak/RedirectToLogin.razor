@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{SSDCPortal.Constants.Settings.LoginPath}?returnurl={Uri.EscapeDataString(Navigation.Uri)}", true);
    }
}
